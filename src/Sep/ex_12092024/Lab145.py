# request
# webdriver (selenium 4)
# csv, os
# pytest, json, allure report, xml, playwright, openpyxml, openpycsv,

# pip -- > pip install library. It is a package manager where we can import other packages into your own projects.

# Automation Project we need-
# selenium, request, csv, os, pytest, json, playright
# we use pip install command to install all package- It allows to installl and manage other packages which are not part of standard libraries
# pip command is also used to uninstall packages
